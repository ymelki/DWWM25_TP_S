{% extends 'base.html.twig' %}

{% block title %}Sproduit{% endblock %}

{% block body %}
    <h1>Sproduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sproduit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sproduit.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sproduit.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ sproduit.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_sproduit_index') }}">back to list</a>

    <a href="{{ path('app_admin_sproduit_edit', {'id': sproduit.id}) }}">edit</a>

    {{ include('admin_sproduit/_delete_form.html.twig') }}
{% endblock %}
