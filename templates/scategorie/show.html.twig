{% extends 'base.html.twig' %}

{% block title %}Scategorie{% endblock %}

{% block body %}
    <h1>Scategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scategorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ scategorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scategorie_index') }}">back to list</a>

    <a href="{{ path('app_scategorie_edit', {'id': scategorie.id}) }}">edit</a>

    {{ include('scategorie/_delete_form.html.twig') }}
{% endblock %}
